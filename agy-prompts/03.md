# agy prompt for lesson 3: Baseline LLM classifier

```bash
agy --print --sandbox --add-dir "$(pwd)" "Design a baseline LLM classifier prompt for one news story. Return JSON schema and list failure modes."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
