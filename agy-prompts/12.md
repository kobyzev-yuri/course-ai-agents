# agy prompt for lesson 12: CrewAI analyst crew

```bash
agy --print --sandbox --add-dir "$(pwd)" "Design CrewAI roles for reviewing chip-news impact labels and map each role to an output."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
