# agy prompt for lesson 11: LangGraph reliability

```bash
agy --print --sandbox --add-dir "$(pwd)" "Design a human review checkpoint for low-confidence strong-impact stories. Return state fields and resume steps."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
