# agy prompt for lesson 2: Схема данных и labels

```bash
agy --print --sandbox --add-dir "$(pwd)" "Review src/news_impact/schema.py and propose two missing validation tests for impact labels."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
