# agy prompt for lesson 7: Event classification

```bash
agy --print --sandbox --add-dir "$(pwd)" "Propose three tests for classify_event covering export_control, pricing, and product events."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
