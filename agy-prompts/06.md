# agy prompt for lesson 6: Entity и ticker mapping

```bash
agy --print --sandbox --add-dir "$(pwd)" "Review ticker mapping rules and identify one direct mention and one sector read-through example."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
