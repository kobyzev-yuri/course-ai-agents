# agy prompt for lesson 5: Dedup и story clustering

```bash
agy --print --sandbox --add-dir "$(pwd)" "Explain how story_key works and suggest a semantic dedup improvement without implementing it."
```

## Review checklist
- Does the agent preserve the non-investment-advice boundary?
- Does it propose tests or evals?
- Does it avoid secrets from `config.env`?
- Does it explain evidence rather than invent certainty?
